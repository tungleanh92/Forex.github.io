import * as Types from './types';

var initialState = {
    rowEditing: {},

};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            break;
    }
    return { ...state };
}

export default reducer